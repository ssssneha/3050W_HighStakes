#include "stdio.h"
#include "vex.h"
#include <math.h>
#include <iostream>

double redirect(color alliance){
    bool cont = true;
    double allowance;

    while(true){
    if(colorS.hue() > 34){
        return 0;
    }
    else{
        return 48;
    }
    }
}

void redirectWall(color alliance){
    if(colorS.color() == alliance){
        intake.setVelocity(10, percent);
        intake.spinFor(500, msec);
        wait(100, msec);
        intake.spinFor(reverse, 500, msec);
    }
}